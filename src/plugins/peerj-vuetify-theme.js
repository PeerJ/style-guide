import PeerJColors from "../journal-colors";
// import colors from "vuetify/lib/util/colors";

const peerjColors = new PeerJColors();

// console.log("v colors", colors);

export default {
  primary: peerjColors.peerj.rgb.hexString,
};
